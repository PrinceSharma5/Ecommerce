<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta charset="utf-8">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        
        <div class="container-xl container p-5 my-5 bg-dark text-white">
        
           <form action="/passwordchanges?tokenn=<%= token %>" method="post" >
               <input type="password"  placeholder="type your new password here" name="newPassword" id="x" required />
               <input type="password"  placeholder="type your password again" name="confirmpassword"  required/>
               <input type="submit"  value="change passsword" />
           </form>
        
        <h5 style="color:white">Go to <a href="/login" style="text-decoration:none">Login</a> Page</h5></div><br>
         <div class="container-xl container p-5">
           <% if(alertt){ %>
             <h1> Password not matches</h1> 
           <% } %>
        <% if(passwordchng){ %>
         <h1><%= passwordchng %></h1>
        <% } %>
        <br>
        
        </div>
    </body>
</html>